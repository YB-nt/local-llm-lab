[project]
name = "local-llm-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "accelerate>=1.14.0",
    "loguru>=0.7.3",
    "torch>=2.13.0",
    "transformers>=5.16.1",
]

[tool.pyright]
reportUnknownMemberType = false
