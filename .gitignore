# 프로젝트 수행도중 메모, 정리를 진행하는 디렉토리
notes/*

# Python / uv
__pycache__/
*.py[cod]
.venv/
.python-version

# !!! 모델 가중치 · 캐시 (절대 커밋 금지) !!!!
models/
*.gguf
*.safetensors
*.bin
.cache/

# 실행 로그 - 결과 .csv는 제외
*.log
bench/*.log
bench/*.csv
!results/*.csv

# OS
.DS_Store
logs/

# 개발 환경
.zed
.ruff_cache
.env
# Claude Code 로컬 설정 (절대경로가 들어가므로 공개 레포에 올리지 않는다)
.claude/settings.local.json
# 런타임 산출물 (bench-memory.sh 의 레거시 30컬럼 CSV + 실행 로그)
benchmarks/
